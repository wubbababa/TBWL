这是一个 Next.js + Supabase 项目，有大量按钮没有实现 onClick
  功能。请设计一个完整的实现方案。

  ## 已有的可复用模式

  ### 1. CSV工具库 (`src/lib/csv.ts`)
  - `generateCsv(rows, columns)` — 数据转CSV
  - `parseCsv(text)` — 解析CSV
  - `downloadCsv(csvContent, filename)` — 触发浏览器下载
  - `generateTemplateCsv(columns)` — 生成模板CSV
  - 已定义的列常量：`EXPORT_COLUMNS`, `IMPORT_COLUMNS`,
  `INVENTORY_APPLY_IMPORT_COLUMNS`, `INVENTORY_PRODUCTS_IMPORT_COLUMNS`,
  `TAIWAN_APPLY_IMPORT_COLUMNS`, `SHIPPING_EXPORT_COLUMNS`,
  `EXPENSE_EXPORT_COLUMNS`, `SCAN_REGISTER_EXPORT_COLUMNS`

  ### 2. CSV导入模态框 (`src/components/orders/CsvImportModal.tsx`)
  - Props: `{ onClose, onImportComplete, tableName, importColumns, title }`
  - 完整的 选择→预览→导入→结果 流程

  ### 3. InventoryActionToolbar
  (`src/components/inventory/InventoryActionToolbar.tsx`)
  - Props: `{ selectedIds, tableName, importColumns, templateFileName,
  title, onActionComplete }`
  - 提供批量删除 + CSV导入 + 模板下载

  ### 4. 订单导出模式 (`src/components/orders/ActionButtons.tsx:39-58`)
  ```ts
  const handleExport = async () => {
    setExporting(true);
    const { data, error } = await
  supabase.from('orders').select('...').order('created_at', { ascending:
  false });
    const csv = generateCsv(data ?? [], EXPORT_COLUMNS);
    downloadCsv(csv, `订单导出_${ts}.csv`);
  };
  ```

  ## 需要实现的按钮（按文件分组）

  ### A. 导出Excel类（3个页面）
  1. ~~`src/app/funds/expense/page.tsx:71` — "导出Excel"~~ ✅ 已实现（2026-06-06）
  2. ~~`src/app/funds/shipping/page.tsx:80` — "导出Excel"~~ ✅ 已实现（2026-06-06）
  3. ~~`src/app/funds/scan-register/page.tsx:72` — "導出Excel"~~ ✅ 已实现（2026-06-07，复用 generateCsv/downloadCsv + 新增 SCAN_REGISTER_EXPORT_COLUMNS）

  ### B. 拷贝至库存商品类（2个页面）
  4. `src/app/inventory/database/page.tsx:56` — "拷贝至库存商品"
  5. `src/app/taiwan/database/page.tsx:59` — "拷贝至臺灣庫存商品"
  这两个页面都查询 `inventory_products` 表，但 database
  是"产品库"，拷贝功能应该是把选中的产品复制到另一个表或创建记录。

  ### C. 台湾库存商品管理（1个页面，4个按钮）
  6. ~~`src/app/taiwan/products/page.tsx:81` —
  "创建臺灣庫存商品"（应打开创建模态框）~~ ✅ 已实现（2026-06-07，复用 CreateInventoryProductModal）
  7. ~~`src/app/taiwan/products/page.tsx:84` — "批量删除"~~ ✅ 已实现（2026-06-07，复用 InventoryActionToolbar + DataTable checkbox）
  8. ~~`src/app/taiwan/products/page.tsx:87` — "Excel批量导入"~~ ✅ 已实现（2026-06-06，复用 CsvImportModal）
  9. ~~`src/app/taiwan/products/page.tsx:90` — "Excel模板下载"~~ ✅ 已实现（2026-06-06，复用 generateTemplateCsv）
  参考：`src/app/inventory/products/page.tsx` 已完整实现了全部功能（使用
  InventoryActionToolbar + CreateInventoryProductModal）

  ### D. 详情查看类（3个页面）
  10. `src/app/inventory/apply/page.tsx:228` — "详情"按钮
  11. `src/app/taiwan/apply/page.tsx:223` — "详情"按钮
  12. `src/app/profit/page.tsx:61` — "详情"按钮

  ### E. 返回导航类（2个页面）
  13. `src/app/inventory/records/page.tsx:70` — "返回"按钮
  14. `src/app/taiwan/records/page.tsx:70` — "返回"按钮

  ### F. 表单提交类
  15. `src/app/other/return-register/page.tsx:85-90` — "重置" + "提交登记"
  这个页面有完整的表单UI但没有state管理和提交逻辑。需要创建完整的表单状态和
  Supabase insert。

  ### G. 索赔管理
  16. `src/app/other/claims/page.tsx:59` — "发起新索赔"
  17. `src/app/other/claims/page.tsx:135` — MoreVertical 操作菜单

  ### H. 退件处理
  18. `src/app/other/returns/page.tsx:51` — "处理"按钮

  ### I. 其他
  19. `src/app/funds/recharge/page.tsx:133` —
  "查看全部"按钮（应跳转或展开）
  20. `src/app/other/parcel-claim/page.tsx:73` — "我的认领"按钮

  ## 设计要求

  1. 每个按钮的实现方案要具体，说明调用什么函数、使用什么已有模式
  2. 对于需要新增 CSV列定义的，在 `src/lib/csv.ts` 中添加
  3. 对于需要新增模态框的，说明是复用已有组件还是新建
  4. 优先复用已有组件（InventoryActionToolbar、CsvImportModal、CreateInvent
  oryProductModal、EditInventoryProductModal）
  5. 分组按实现难度和依赖关系排序

  请输出一个按分组的实现方案，每个按钮包含：文件路径、按钮文本、实现方式、
  依赖的组件/函数。