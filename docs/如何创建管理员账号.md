# 如何创建管理员账号

在 Supabase SQL Editor 中执行：

```sql
UPDATE auth.users
SET raw_app_meta_data = raw_app_meta_data || '{"role":"admin"}'::jsonb
WHERE email = 'qq@qq.com';
```

把邮箱替换为你当前登录账号的邮箱即可。执行后该账号就拥有管理员权限，可以创建/禁用其他账号。
